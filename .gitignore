node_modules/
dist/
.DS_Store
*.log

# sqlite database (accounts, rovers, handshakes)
data/

# build artifacts
server.js
*.tsbuildinfo

# local smoke test artifacts
shot-*.png

# local env (holds the shared OpenRouter test key — never commit)
freeknet.env
*.env
